/*
class to hold the pathfinding algorithm by use of the agent
*/

class Pathfinder {
  
  Pathfinder(){
  }
  
  // returns the index of the closest stall to an agent given their location and the toVisit Stall list
  // returns -1 if node not found
  //
  //int getClosestStall(PVector a, ArrayList<Stall> toVisit) {
  //  int node = -1;
  //  float distance = Float.MAX_VALUE;
  //  float currentDist;
    
  //  for (int i=0; i<toVisit.size(); i++) {
  //    currentDist = sqrt( sq(a.x-getNode(i).x) + sq(a.y-getNode(i).y) );
  //    if (currentDist < distance) {
  //      node = i;
  //      distance = currentDist;
  //    }
  //  }
    
  //  return node;
  //}
  
  //PVector getStallLoc(){}
  
}
